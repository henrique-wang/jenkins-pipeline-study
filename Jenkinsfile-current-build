pipeline {
  agent any
  stages {
    stage('Hello') {
      steps {
        sh 'echo Hello World bugfix'
        echo "Build number is ${currentBuild.number}"
        echo "Branch name ${env.BRANCH_NAME}"
      }
    }
  }
  post {
    always {
      echo currentBuild.currentResult
    }
  }
}